# Architecture diagrams

Store draw.io sources and exported PNGs here.

| File | Describes |
|------|-----------|
| `portfolio-demos-aws.drawio` | CDK stack (API GW, Lambda, Bedrock, S3, Secrets Manager) |
| `tableau-knowledge-platform.drawio` | TWB → metadata → Bedrock → S3 → MCP |
| `mcp-query-governance.drawio` | Sentinel + governed MCP (future AWS stack) |

Generate with the **drawio** MCP server — see [`../DRAWIO_MCP.md`](../DRAWIO_MCP.md).
